# Aplicação com electron + React js

Instale as dependências com 'yarn install'

para executar no modo de desenvolvedor siga os comandos de scripts
pré-configurados no package.json - para build do projetos também.